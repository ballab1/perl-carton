version: '3.3'

services:
    carton:
        build:
            args:
                CBF_VERSION: "${CBF_VERSION:-}"
                CONTAINER_NAME: perl-carton
                CONTAINER_VERSION: ${CONTAINER_VERSION:-3.0.0}
                FROM_BASE: perl-5.26.2:${CONTAINER_TAG:-latest}
            cache_from:
                - alpine:3.8
                - base_container:${CONTAINER_TAG:-latest}
                - perl-5.26.2:${CONTAINER_TAG:-latest}
            context: .
            dockerfile: Dockerfile
        container_name: carton
        environment:
            DEBUG_TRACE: ${DEBUG_TRACE:-0}
        image: perl-carton:${CONTAINER_TAG:-latest}